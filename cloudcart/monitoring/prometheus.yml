global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'node_backend'
    # Assuming you add a prometheus exporter to Node.js backend on port 5000
    static_configs:
      - targets: ['backend:5000']
